# Testing

Run the local checks with:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\gradle.ps1 check
```

As real Hubitat code is imported, add focused tests under `src/test/groovy`.
